package game.gelos;

enum GeloPopType {
	/**
	 * Stops the popping animation.
	 */
	NONE;

	/**
	 * The flashing animation used in most gamemodes. The "frightened" face
	 * only display for a couple of frames.
	 */
	TSU;

	/**
	 * The shrinking animation used when animation
	 */
	FEVER;
}
