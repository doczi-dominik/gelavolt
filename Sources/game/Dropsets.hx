package game;

import game.gelogroups.GeloGroupType;

/**
 * A static collection of dropsets.
 */
class Dropsets {
	public static final CLASSICAL = [
		PAIR, PAIR, PAIR, PAIR, PAIR, PAIR, PAIR, PAIR, PAIR, PAIR, PAIR, PAIR, PAIR, PAIR, PAIR, PAIR
	];
}
